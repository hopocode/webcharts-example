# Webgraph — demo projekt

Krátký demo projekt podle `zadani1.md`.

Instalace a spuštění:

```bash
cd /Users/honzap/dev/webgraph
npm install
npm run dev
```

Otevřete v prohlížeči `http://localhost:5173/default` pro zobrazení stránky s komponentou `CashFlowBarChart`.
